using System;

namespace Linq
{
    public enum TipoFreteEnum
    {
        Cif = 'C', 
        Fob = 'F'
    }

    public enum StatusEnum
    {
        Ativo = 'A',
        Faturado = 'F', 
        Cancelado = 'C'
    }
}
